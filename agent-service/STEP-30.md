# Step 30 - 前端 Agent 工作台

本步新增 Vue 工作台并接入 Java 同域 API。

- 支持新建/切换会话、输入自然语言需求、停止和重新执行任务。
- 订阅具名 SSE 事件，依靠 `Last-Event-ID` 自动续传；刷新后从任务事件历史重建答案。
- 展示可验证的阶段、工具摘要和图片引用，不展示隐藏思维链。
- 引用使用字符串 pictureId 跳转站内详情页，避免 bigint 精度损失。
- 可通过 `?spaceId=` 固定会话空间范围；浏览器只携带现有登录 Cookie，不接触服务密钥。
