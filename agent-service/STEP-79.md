# Step 79：恢复/回放引用撤权保护

- 检索节点已完成的checkpoint恢复前重新授权全部引用，任一图片失效则拒绝输出旧答案，避免跳过检索节点带出旧元数据。
- Java引用写入、答案追加、历史/SSE回放增加已引用图片当前权限复核，覆盖公共图片撤回审核、删除或空间变化。
- 复核按20张分批，最多200条引用事件；超界或引用格式异常拒绝访问，不静默略过。已交付给浏览器的历史数据无法被服务端撤回。
- 新增恢复撤权及Java引用校验测试。本步骤只加强读取保护，不修改已有图片或历史记录。
