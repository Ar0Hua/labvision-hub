# Step 37：混合结构化过滤

## 对应文档

- P0 / FR-01：自然语言与结构化限制条件混合检索。
- 阶段 2：SQL 结构化召回、向量 payload filter 和权限范围解析。

## 本步完成

- 扩展严格 `SearchIntent`：图片格式、创建日期区间、最小宽高、最大文件大小及新旧排序。
- 千问只允许输出固定 JSON 字段；Pydantic 校验枚举、范围、日期先后关系并规范化格式。
- Python→Java 内部请求仅透传显式白名单字段，不接受 raw SQL、任意字段或任意工具名。
- Java 在任务签名绑定的公共图库/空间范围内构造参数化 MyBatis 条件，并再次校验所有输入上限。
- 同一套分类、标签、格式、时间、尺寸条件同步加入 Qdrant 文本和图像向量查询，避免融合结果绕过过滤条件。
- Qdrant filter 固定包含 `scopeKey` 与未删除状态；公共图库额外要求审核通过。
- 为宽度、高度和文件大小增加整数 payload index。

## 验证

- Python：`44 passed`，覆盖模型结构校验、白名单透传、SQL/向量过滤一致性。
- Java Agent：`43 tests`，全部通过，覆盖空间约束、参数化条件和非法范围拒绝。
- 本步未修改前端，沿用 Step 36 已通过的生产构建。
- 本步没有执行数据库迁移，也没有连接真实 Qdrant、DashScope 或 MySQL 数据集调优。
