# Step 33 - Agent 运行指标

- `/metrics` 输出 Prometheus 文本格式的任务结果、总耗时和空检索计数。
- 结果标签使用固定白名单，禁止把查询、用户、会话、图片或异常文本写入标签。
- 指标采集覆盖成功、失败、取消、超时、执行器不可用和忽略的重复/非待处理任务。
- 当前实现是单进程内指标；多 Worker 部署时由部署环境选择共享 collector 或逐实例抓取。
