# Step 78：反馈导出到离线复核

- JSON报告在任务/引用再次鉴权后同步读取当前用户反馈，保存pictureId和label；不附带账号、签名URL或数据库访问凭据。
- 新增 `python -m app.evaluation.feedback report.json` 输出只含排名和反馈的人工复核草案，不回显问题或图像描述。
- 草案必须人工复核才能进入golden set；权限异常反馈不能直接当作已证实越权，不自动训练或调整在线权重。
- 新增反馈引用绑定、权限异常不自动升级标签和敏感文字不导出测试；补齐单图基础元数据宽高比。
