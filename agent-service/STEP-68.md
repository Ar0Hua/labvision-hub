# Step 68：全部授权空间检索

- 新建全范围会话入口（/agent?scope=all），包含创建时全部可查看空间及已审核公共图；原公共/单空间会话语义保持不变。
- Java 从用户所属/管理候选空间逐项按既有 RBAC 解析，最多50个已授权空间；超过容量明确拒绝而非静默截断。候选枚举安全上限500。
- 持久化不可由模型修改的空间清单，执行及读取历史时重验整个清单；任一空间撤权阻止旧会话读取，新增权限需新建会话。
- SQL OR 范围与 Qdrant 分支过滤统一；公共分支单独要求审核通过，私有分支不误用公共审核条件。最终图片及签名视觉输入仍再鉴权。
- 全范围图片组矩阵支持相同范围；全范围统计要求切换具体空间或明确比较 ID，不用公共图统计冒充全局数据。
- 新增 V012 会话范围字段迁移，未执行；部署前必须迁移。Python 96 项、Java Agent 回归、前端构建通过。
- 仍待真实 DB/权限变更并发联调。全范围快照是一致性与安全边界，不自动扩张历史任务权限。
