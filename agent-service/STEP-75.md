# Step 75：运行边界与单次结构修复

- 环境配置启动时验证模型HTTPS地址与运维管理的主机白名单；禁止HTTP、凭据、非443端口、查询串和伪后缀主机。默认只批准 dashscope.aliyuncs.com，切换地域需显式扩展白名单。
- 意图JSON无效时最多重新生成一次，再失败走确定性回退；修复消耗同一任务预算，不把无效模型文本重新作为提示词输入。
- LangGraph三节点有向无环图增加显式 recursion_limit（AGENT_MAX_STEPS，默认6），既有工具/模型/时间预算继续生效。此图不允许模型自建循环。
- 新增白名单、修复次数、恶意响应不回灌和图步数测试。供应商不训练条款/地域和数据等级由部署者依据账号合同确认，代码不能替代合同保证。
