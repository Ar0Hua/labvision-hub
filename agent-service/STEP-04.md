# 会话空间范围绑定

创建接口 POST /api/agent/conversations 支持可选 JSON 请求体：{"spaceId":"7"}。
未指定空间时仅公共图库；指定时先复核当前用户查看权限，再持久化绑定。
图片详情访问必须同时满足会话归属、会话空间一致与实时资源权限。
当前子步骤实现单空间，后续多空间会话仍待实现；不隐式扩大为全库。

V002__conversation_scope.sql 应在 V001 后手动执行一次；本次未执行迁移。
旧会话 spaceId 为 NULL，按公共图库处理，访问私有或团队空间需创建新会话。
测试包含同空间、跨空间、公共/私有互相隔离、创建无权限时不写数据库。
仍待实现消息持久化、任务恢复、受控内部工具和模型接入。
