# Step 73：结果元数据和短期缩略图

- 引用卡片按当前会话权限加载空间、标签、尺寸、上传时间/用户与格式。
- 用户主动查看缩略图时再次鉴权并签发120秒URL；不持久化到消息、报告或浏览器存储，前端到期清除。
- 复用已有对象地址白名单签名逻辑，不新增任意URL代理；前端只接受无内嵌凭据的HTTPS地址。
- 现有历史事件不包含签名图片URL；权限变化时卡片读取失败不展示新预览。
- 与所有签名URL机制相同，已发出的URL在短TTL内仍可能可用；不是即时吊销机制。
