# Step 76：外部调用熔断与保守重试

- 任务和索引Worker默认HTTP客户端接入按origin有界熔断：连续5次网络/429/5xx失败后30秒冷却，单半开探针，最多32个origin状态。
- 仅连接建立失败最多重试一次，带指数公式与随机抖动，并纳入工具预算；读写超时、已发起模型生成、SSE中断与HTTP错误不透明重放，避免重复计费或追加事件。
- 流读取失败计入熔断，成功消费响应后关闭熔断。保留既有HTTP超时、任务取消、总时限与outbox有界失败重试。
- 自动测试覆盖重试类别/次数、熔断冷却恢复和状态内存上限。未访问真实供应商或部署。
