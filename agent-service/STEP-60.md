# Step 60：融合候选时间排序

- 修正 SQL 时间顺序被 RRF 覆盖的问题：融合最多 50 项候选，最终分批鉴权后按最新/最早排序，再折叠重复并截取请求数量。
- Java 每批最多鉴权 20 张，不扩大现有接口权限或参数上限；使用最终鉴权返回的当前元数据。
- 支持 ISO 日期、时区偏移和 Java 毫秒时间戳；无时区日期按 UTC+8，相同时间保持融合顺序，无效或缺失时间置后。
- 回答明确说明仅在本次有界召回候选内排序，不宣称全库时间排名。
- Python 全量 78 项测试通过，覆盖撤权过滤、鉴权后元数据更新、去重后截取及分批鉴权。
- 未部署、未执行迁移。本步骤未补齐上传人/颜色/亮度/宽高比筛选，也未新增 reranker。
