# Step 62：宽高比结构化检索

- 增加 minAspectRatio/maxAspectRatio（宽÷高，0.01～100），拒绝非有限值和倒置范围。
- SQL 使用参数绑定与零高度保护，保留原空间/审核/删除权限范围。
- Qdrant 新增 aspectRatio 浮点 payload 索引，Worker 从事实宽高计算；文本与图像召回共享过滤器。
- 最终鉴权后按当前宽高复核，缺失尺寸不能视为满足比例条件；多轮可继承或重置。
- Python 85 项及 Java Agent 回归通过；新增比例边界、最终复核、SQL 参数绑定及索引契约测试。
- 部署时需执行既有 schema 初始化及历史索引回填，旧向量缺少比例字段会被范围过滤排除。本轮未部署、未回填、未执行迁移。
- P0 仍有亮度/颜色、临时图片、统一授权空间检索、重排、部分成果与预算等未完成项。
