# Step 49：矩阵展示与 Markdown/JSON 报告下载

- Vue 安全插值渲染 Markdown 表格，模型内容不作为 HTML 执行。
- 成功任务可导出包含任务、范围、时间、问题、回答、引用和限制说明的报告。
- 导出前重新校验任务及图片权限，拒绝失权引用；去除外部 URL，保留图片 ID。
- Vite 生产构建验证；仍保留原有 GlobalHeader 类型导入及大包警告。
- 当前为浏览器本地导出，PDF 和服务端持久化报告尚未实现。
