# Step 70：临时图片检索与分析

- 新增用户/会话绑定的 PNG/JPEG 上传与删除接口：5MB、16M像素限制，解码前检查尺寸，重编码去元数据，最长边1024，Redis TTL 900秒，每用户15分钟5次上传配额。
- V013 为任务添加临时输入引用；提交与手动重试验证所有权/TTL，内部读取仅从已签名任务取引用。迁移未执行。
- Python 临时输入不会序列化到 checkpoint；图像 embedding 只查询当前授权空间索引，不写永久图片/向量。结果仍经 Java 再鉴权。
- 支持临时单图视觉描述，不生成永久 pictureId；模型/向量不可用明确降级。任务输入不可同时混用平台样例和临时图。
- 工作台支持上传、移除、过期提示和云端用途说明；临时缓存使用现有 Redis，重试过期输入需重新上传。
- Python 103 项、Java Agent 回归及 Vite build-only 通过。新增解码、数据序列化、只读向量请求、伪造引用拒绝测试。保留前端既有构建警告。
- 未运行真实模型、Redis 上传联调或部署；实例级上传并发/总缓存配额需结合部署容量配置，临时缓存应启用访问控制与传输保护。
