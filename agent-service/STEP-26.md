# Step 26 - 视觉模型安全图片入口

本步在 Java 权限事实源中增加视觉分析专用的图片入口。

- 每次请求先验证内部任务令牌、用户/会话归属和图片当前读取权限。
- 单批最多 8 张，只签发 120 秒有效的 COS GET 地址。
- 优先发送缩略图，避免无必要地把原图交给外部模型。
- 签名器拒绝非当前 COS 域名、路径穿越和异常有效期。
- Python/前端无法用任意 URL 绕过 Java 权限事实源。

本步不调用 DashScope、不读取真实图片，也不修改 COS 数据。
