# Step 61：上传人结构化检索

- SearchIntent 增加 uploaderId，使用正整数十进制字符串并限制为 Java Long 范围，避免 ID 精度丢失；提示词禁止由姓名猜测 ID。
- Python 到 Java 的白名单契约透传该字段；MySQL 使用参数绑定追加 userId 条件，原空间、审核及删除条件不变。
- Qdrant 文本/图像召回共用 userId payload 过滤；复用已有索引字段，无新增迁移。
- 最终鉴权后复核当前上传人，剔除字段缺失或不匹配的过期候选。
- 多轮条件可保留，重置时清空；新增严格 ID、向量范围、跨服务契约、最终复核及 Java 参数化查询测试。
- Python 82 项通过；Java Agent 回归结果见开发记录。
- 仅支持明确上传人 ID，暂不解析用户名或“我”的身份映射；颜色、亮度、宽高比仍待开发。未部署、未执行迁移。
